const randomKey = Math.round(Math.random() * (999 - 100)) + 100;

let lyricCard = [
    {
        key: randomKey,
        category: 'lyric',
        name: '',
        image: '',
        price: 0,
        description: ''
    }
]

export default lyricCard;