const randomKey = Math.round(Math.random() * (999 - 100)) + 100;

let portrait = [
    {
        key: randomKey,
        category: 'portrait',
        name: '',
        image: '',
        price: 0,
        description: ''
    }
]

export default portrait;